## Commands to know

```go
go run ./cmd/td # run without building (like tsx src/index.ts)

go build ./cmd/td # compile to a binary

go install ./cmd/td    # compile + install to $GOPATH/bin (makes td global)
```
